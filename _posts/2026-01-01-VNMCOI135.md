---
layout: post
title:  "VNMCOI135"
categories: []
code: VNMCOI135
src: VNMCOI135.cpp
---

Ta có thể thấy ta có thể áp dụng sử dụng Dijkstra trên lưới nhưng vấn đề là với $n$ khoảng $2000$ thì khi đó độ phức tạp của Dijkstra trên lưới là $\mathcal{O}((V + E) \times \log(V))$ thì $V = N^2 \times 4$ còn $E$ là số lượng trường hợp có thể của từng đỉnh với mỗi đỉnh đi được $3$ hướng là $N^2 \times 3$.

**Vấn đề:** Vậy chúng ta cần một cách để duyệt nhanh hơn (vì phần lớn lí do Dijkstra chậm là vì sử dụng `priority_queue`).

**Giải pháp:** Chúng ta thực hiện khai thác điểm mấu chốt của BFS là:

Bản chất thì Dijkstra chính là BFS nhưng cần phải duyệt lại nhiều lần do Dijkstra không biết được trong tương lai một đỉnh hoặc cấu hình có thể lại xuất hiện trong queue (tức là lại được tối ưu hóa nữa vì trọng số cạnh là một con số ngẫu nhiên).

Nhưng tính chất của bài toán này lại khác: Ta thấy được trong các cấu hình thì ta chỉ có hai trường hợp là $0$ hoặc $1$ vậy ta có thể vận dụng tính chất này để giảm lượng cấu hình phải duyệt bằng cách các cấu hình tối ưu $0$ chúng ta sẽ ưu tiên duyệt còn các cấu hình $1$ hình chúng ta duyệt sau điều này vẫn đảm bảo tính chất đúng của BFS trên đồ thị vô hướng là:

Nếu là đồ thị vô hướng không trọng số thì BFS đi đến đâu thì đảm bảo là đỉnh đó là kết quả tối ưu nhưng trên đường đi có $0$ hoặc $1$ thì nếu chúng ta ở các cấu hình $0$ mới đảm bảo tối ưu còn cấu hình $1$ thì phải kiểm qua lại.

Vậy thuật toán của chúng ta đã giảm được phần không cần sử dụng `priority_queue` mà chúng ta sẽ sử dụng `deque` vì cần phải đảm bảo tính đúng đắn của việc duyệt $0$ trước $1$ nên ta phải đẩy $0$ vào hàng đợi đầu và đẩy $1$ vào hàng đợi cuối (giống như `priority_queue` duyệt các cấu hình có trọng số thấp nhất thì ta cũng duyệt nhưng duyệt cấu hình thấp là $0$ trước rồi mới $1$).