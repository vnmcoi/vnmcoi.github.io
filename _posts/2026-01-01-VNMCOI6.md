---
layout: post
title:  "VNMCOI6"
categories: []
code: VNMCOI6
src: VNMCOI6.cpp
---

<!--more-->

Dễ thấy với mỗi ô `A[i]` đã cho trong trường hợp tệ nhất chúng ta phải tốn `1` thao tác cho chúng vậy chúng ta phải tiêu tốn `N` thao tác tối đa.

Vậy bài toán bây giờ thay vì là xác định số lượng truy vấn chúng ta chỉ cần đếm số lượng truy vấn không cần thiết.

Hãy cùng xét trường hợp bên dưới:

- `2 3 2`: Trong đây chúng ta sẽ thực hiện truy vấn `[1; 3]` với `2` viên gạch vậy thì ô thứ `3` với `2` viên gạch cũng sẽ bị hoàn thành.

Vậy chúng ta rút ra được đầu tiên là nếu như trước `A[i]` có `A[j]` thỏa mãn `A[j] = A[i]` và `j < i` và các phần tử nằm giữa chúng không có phần tử nào có giá trị bé hơn `A[i]`.

# Subtask 1 và 2

Với hai subtask này chúng ta có thể sử dụng IT để tìm kiếm nhanh bên trái của phần tử đang xét có phần tử nào hợp lệ không.

# Subtask 3

Với subtask này việc `N` tối đa lên đến `2000000` phần tử việc sử dụng IT bây giờ là bất khả thi nên chúng ta sẽ sử dụng stack đơn điệu để xử lý nhanh các phần tử max trong một đoạn tịnh tiến.