---
layout: post
title:  "VNMCOI2"
categories: [prefix_sum, range_queries]
code: VNMCOI2
src: VNMCOI2.cpp
---

<!--more-->

Chúng ta phải tìm cách để tối ưu việc tính bằng cách giảm số lần duyệt mảng lại của mỗi truy vấn.

Trong đó chúng ta sẽ thấy để tính tổng của đoạn `[l; r]` thì chúng ta có thể tính bằng cách tính tổng của đoạn `[1; r]` và `[1; l - 1]` vì `tổng của đoạn [l; r] = tổng của đoạn [1; r] - tổng của đoạn [1; l - 1]`.

Vậy chúng ta gọi mảng `pref` là mảng tổng tiền tố (tức sẽ lưu tổng từ vị trí `1` đến `i` hay `pref[i] = tổng của đoạn [1; i]`).

Khi đó tổng của truy vấn `[l; r]` chính là `pref[r] - pref[l - 1]`.