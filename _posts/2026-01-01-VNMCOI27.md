---
layout: post
title:  "VNMCOI27"
categories: []
code: VNMCOI27
src: VNMCOI27.cpp
---

<!--more-->

Với bài này chúng ta có thể sử dụng `for` hoặc `while` tùy ý.

Code mẫu cho `while`:

```

#include <bits/stdc++.h>
using namespace std;

int N;

int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0);

    cin >> N;
    int i = (N / 2) * 2;
    while (i >= 2)
    {
        cout << i << ' ';
        i -= 2;
    }
    return 0;
}

```